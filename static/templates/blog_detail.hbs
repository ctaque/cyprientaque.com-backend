
{{#*inline "page"}}
<div class="pageWrap">
    <div class="page">
        <div class="article">
            <div class="page-title">
                <div class="date-title-outer">
                    <div class="top">
                        <div><span>Published {{format_date article.created_at "%b"}}</span>&nbsp;<span>{{format_date article.created_at "%Y"}}</span></div>
                    </div>
                    <h1 class="title">
                        {{article.title}}
                    </h1>
                </div>
                <div>
                    {{#if article.tags_list}}
                    <div class="tags-list">
                        {{#each article.tags_list}}
                            <a href="/blog?tag={{this}}" class="tag">{{this}}</a>
                        {{/each}}
                    </div>
                    <div class="separator-h"></div>
                    {{/if}}
                </div>
            </div>
            <div class="header">
                {{#if article.primary_image }}
                    <img class="banner" src={{article.primary_image.w1500_object_url}}>
                {{/if }}
                <div class="gradient"></div>
            </div>
            <div class="">
                <div class="content">
                    {{render_markdown article.content}}
                </div>
            </div>
            <div class="footer">
                {{#if article.updated_at}}
                    <span class="">Last edited : {{format_date article.updated_at "%Y-%m-%d %H:%M:%S"}}</span>
                {{/if }}
                <div id="addLike"></div>
            </div>
            <script>
            var app = Elm.AddLike.init({
                node: document.getElementById('addLike'),
                flags: {
                    views_count: {{article.likes_count}},
                    project_id: {{article.id}}
                }
            });
            </script>
        </div>
    </div>
</div>
{{/inline}}

{{~> (base)~}}
